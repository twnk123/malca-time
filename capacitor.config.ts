import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.malcatime.app',
  appName: 'MalcaTime',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;